Drop character images in this folder, then set the matching "image" field
in characters_data.js to the file's relative path, e.g.:

  image: 'images/snake-eyes.jpg'

Leave it as `null` for characters you don't have art for yet — the site
will keep showing the placeholder box until you fill it in.

Recommended: roughly square images (they're cropped to a square automatically).
